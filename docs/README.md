# Docs assets

Drop a screenshot of the running app here as `screenshot.png` (referenced by the
top‑level README). A 16:10 capture of the full three‑panel interface with the demo
data loaded works well.
